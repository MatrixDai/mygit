name: Release
on:
  workflow_dispatch:

jobs:
  release:
    permissions: write-all
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Test
      run: |
        echo "start"
        echo "$(git rev-parse --short HEAD)"
        echo "end"
